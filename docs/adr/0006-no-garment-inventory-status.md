# 用户产品不再有衣物库存状态

唯一用户产品是微信小程序，且不做待洗、收纳、清洁、需修补、已归档。衣服在衣橱里就是能拿来穿。

本轮拆除小程序接口上的状态字段与分叉（含 `status` / `statusLabel` / `wearableCount`）、给 AI 的 status，以及网页衣橱的状态下拉、筛选和展示；不拆除整个网页应用。管理员 Excel 与备份 zip 不再读写状态，导入时忽略旧字段。

数据库 `status` 列先保留：旧行全部改为可穿，新行默认仍是可穿。本轮不 drop 列。
